# FamilyPTT R12 registration retry

Purpose: harmless source-change marker used to force the existing AFZ updater to restart the agent wrapper so the already-present `AFZ FamilyPTT Edge Preflight Watcher R12` scheduled-task registration runs again.

This file contains no credentials, commands, DNS changes, proxy changes, certificate changes, firewall changes, or backend changes.
