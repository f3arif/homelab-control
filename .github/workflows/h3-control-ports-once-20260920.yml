name: H3 Control Ports Once 20260920

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/h3-control-ports-once-20260920.yml'

permissions:
  contents: read
  id-token: write

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Guard
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_RUN_ATTEMPT" = '1'
      - name: Join AFZ tailnet
        uses: tailscale/github-action@306e68a486fd2350f2bfc3b19fcd143891a4a2d8
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          audience: ${{ secrets.TS_AUDIENCE }}
          tags: tag:afz-deploy
      - name: Probe H3 read only
        shell: bash
        run: |
          set +e
          h=100.106.186.118
          echo "TAILSCALE_STATUS"
          tailscale status
          tailscale ping -c 1 --timeout=5s "$h" || true
          for p in 22 80 443 4533 5149 8087 8788 8789 8790 8791 8792 8793 8794 8795 8796 8797 9000 3000; do
            if timeout 2 bash -c "</dev/tcp/$h/$p" 2>/dev/null; then
              echo "PORT_$p=OPEN"
            else
              echo "PORT_$p=CLOSED"
            fi
          done
          for u in http://$h:5149/api/system/health http://$h:8788/health http://$h:8789/health http://$h:8791/ http://$h:8795/health http://$h:8796/health http://$h:8797/health http://$h:8087/status/hpenvy; do
            echo "GET=$u"
            curl -sS --connect-timeout 2 --max-time 5 -i "$u" | sed -n '1,35p' || true
          done
          echo "FINAL=READ_ONLY"
