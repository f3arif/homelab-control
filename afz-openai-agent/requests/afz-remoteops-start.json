{
  "id": "afz-remoteops-start-20260831-radiohilal-edge-promo-resume-r1",
  "taskName": "AFZ Remote Ops",
  "purpose": "Start the existing AFZ Remote Ops scheduled task once so the already-queued RadioHilal ReadyForReview safe edge-promo candidate processor gets another scheduling pass. Do not create or duplicate the RadioHilal queue item, do not bypass CPU/RAM resource guards, do not approve or alter any Original, and allow only the pre-existing bounded candidate processor to create separate Candidates when edge-only promo removal is safe; otherwise leave the item for human review. OneDrive is backup-only."
}
