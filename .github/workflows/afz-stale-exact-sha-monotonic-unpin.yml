name: AFZ Stale Exact SHA Monotonic Unpin (Retired)

# RETIRED 2026-09-04
# This was a one-shot bootstrap workflow. The monotonic exact-SHA behavior is
# already part of the current control path, while this historical bootstrap
# remained pinned to an obsolete deploy signal and produced zero-job failures.
# Keep the file for audit/history, but do not run it automatically.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired bootstrap notice
        run: |
          echo 'AFZ_STALE_EXACT_SHA_MONOTONIC_UNPIN=RETIRED'
          echo 'No control-plane or worker mutation performed.'
